{
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "echo Backend running at http://localhost:8000 && cd backend && python manage.py runserver localhost:8000",
    "frontend": "echo Frontend running at http://localhost:5173 && cd frontend && npm run dev -- --host localhost",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
